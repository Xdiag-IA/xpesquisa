[build-system]
requires = ["setuptools>=77,<84"]
build-backend = "setuptools.build_meta"

[project]
name = "xpesquisa"
version = "0.2.0"
description = "Pesquisa científica aberta para a saúde brasileira"
readme = "README.md"
requires-python = ">=3.10"
license = "Apache-2.0"
authors = [{name = "Xdiag Tecnologias"}]
dependencies = ["fastapi>=0.115,<1", "pydantic>=2.10,<3", "httpx>=0.28,<0.29", "uvicorn>=0.34,<1"]

[project.optional-dependencies]
dev = ["pytest>=8,<10"]

[project.scripts]
xpesquisa = "xpesquisa.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
xpesquisa = ["web/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
